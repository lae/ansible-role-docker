---
- hosts: localhost
  connection: local
  roles:
    - lae.travis-lxc
  vars:
    test_profiles:
      - profile: debian-buster
    test_host_suffixes:
      - standard
      - python
      - compose
    lxc_use_overlayfs: no

- hosts: docker_python:docker_compose
  roles:
    - lae.pip
